name: Test and lint

on:
  push:
  workflow_call:

permissions:
  contents: read

jobs:
  test-lint:
    name: Test and lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup
      - run: bun run test
      - run: bun run lint
